{
  "name": "rumies",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "browser-sync start --server --files '*.html' --port 8080 --open"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3"
  }
}
